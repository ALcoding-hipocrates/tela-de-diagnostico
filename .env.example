# Copie pra .env.local (já no gitignore) e preencha pra ativar a análise por LLM.
# Sem a chave, a UI continua funcionando com mocks e detecção local de red flags.
#
# ⚠ Uso DEV/DEMO apenas — a chave fica exposta no bundle do browser.
# Em produção, proxiar via backend (nunca expor chave no client).
VITE_ANTHROPIC_API_KEY=
